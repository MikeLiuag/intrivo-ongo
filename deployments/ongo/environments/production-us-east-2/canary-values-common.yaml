image:
  tag: production-latest

testConnection: false

host: ongo-canary.intrivo.com

envCommon:
- name: NODE_ENV
  value: production

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 20
